apiVersion: apps/v1
kind: Deployment
metadata:
  name: ruggine
spec:
  selector:
    matchLabels:
      app: ruggine
      environment: staging
  template:
    metadata:
      labels:
        app: ruggine
        environment: staging
---
apiVersion: v1
kind: Service
metadata:
  name: ruggine-service
spec:
  selector:
    app: ruggine
    environment: staging
